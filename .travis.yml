language: php

before_script:
  - ./tests/enable-php-extensions.sh
  - composer update --dev

php:
  - 5.3
  - 5.4
  - 5.5

before_script:
  - composer self-update
  - composer install --prefer-source --no-interaction --dev

script:
  - php vendor/bin/phpcs --standard=PSR0 --encoding=utf-8 src/
  - phpunit --configuration phpunit.xml.travisci